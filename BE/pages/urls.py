from django.urls import path
# from .views import (
    
# )

app_name = "pages"

urlpatterns = [
    # path('', ?.as_view(), name='?'), #   페이지 리스트
    # path('', ?.as_view(), name='?'), # /<id>/  페이지 상세 정보 + 게시판
    # path('', ?.as_view(), name='?'), # /<id>/post/new  페이지에 게시글 작성
]