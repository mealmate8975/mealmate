from django.urls import path
# from .views import (
    
# )

app_name = "posts"

urlpatterns = [
    # path('', ?.as_view(), name='?'),      #전체 포스트 피드
    # path('', ?.as_view(), name='?'),      # /<id>/     게시글 상세
]